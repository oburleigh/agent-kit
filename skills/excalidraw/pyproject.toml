[project]
name = "excalidraw-skill"
version = "2.0.0"
description = "Excalidraw diagram skill — layout, render, Mermaid via Playwright"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.40.0",
]

[tool.uv]
